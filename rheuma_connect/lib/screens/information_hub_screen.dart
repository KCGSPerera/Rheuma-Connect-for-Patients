import 'package:flutter/material.dart';

class InformationHubScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Information Hub')),
      body: Center(
        child: const Text('Information Hub Content Goes Here'),
      ),
    );
  }
}
